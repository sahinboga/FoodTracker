import { CountinesFilterByCityidPipe } from './countines-filter-by-cityid.pipe';

describe('CountinesFilterByCityidPipe', () => {
  it('create an instance', () => {
    const pipe = new CountinesFilterByCityidPipe();
    expect(pipe).toBeTruthy();
  });
});
